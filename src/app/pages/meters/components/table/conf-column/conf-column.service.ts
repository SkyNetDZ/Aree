import {Injectable} from '@angular/core';
import {Http, Jsonp, Headers} from '@angular/http';

@Injectable()
export class ConfColumnService {

  constructor(private _jsonp: Jsonp, private _http: Http) {
  }

  saveConfig() {

  }

  loadConfg() {

  }
}
